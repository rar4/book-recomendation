@page "/feed"
<PageTitle>Feed</PageTitle>

<nav></nav>

<img class="bookCover" src="@bookCover" alt="Book cover">

<h2 class="bookTitle">@bookTitle</h2>

<p class="bookInfo">@bookDescription</p>


<div class="starWrapper">

    @for (byte i = 1; i < 6; i++)
        {   
            byte star = i;
            <span class="bookStar" @onclick="@( () => {Rating=i;})">★</span>
        }

</div>
   




@code{
    string bookCover = "https://img.freepik.com/free-vector/stack-colojpg?semt=ais_hybrid&w=740&q=80";
    string bookTitle = "lorem";
    string bookDescription = "lorem lorem a a a a a  a a aa fds f sdf sdf sd  f d f  dfdfd  fdf ";
    private byte Rating = 0;


}